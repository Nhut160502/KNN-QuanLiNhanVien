import { getListUsers, showUser, storeUser, updateUser, destroyUser } from './user'
import { getListSalaries, showSalary, storeSalary, updateSalary, destroySalary } from './salary'
import { getListBonus, showBonus, storeBonus, updateBonus, destroyBonus } from './bonus'
import {
  getListDepartment,
  showDepartment,
  storeDepartment,
  updateDepartment,
  destroyDepartment,
} from './department'
import {
  getListPosition,
  showPosition,
  storePosition,
  updatePosition,
  destroyPosition,
} from './position'

export {
  getListUsers,
  showUser,
  storeUser,
  updateUser,
  destroyUser,
  getListSalaries,
  showSalary,
  storeSalary,
  updateSalary,
  destroySalary,
  getListBonus,
  showBonus,
  storeBonus,
  updateBonus,
  destroyBonus,
  getListDepartment,
  showDepartment,
  storeDepartment,
  updateDepartment,
  destroyDepartment,
  getListPosition,
  showPosition,
  storePosition,
  updatePosition,
  destroyPosition,
}
